version: "3.7"
services:
  node-app:
    build:
      context: .
      args:
        - NODE_ENV=development
    volumes:
      - ./Natours:/app/src:ro
    environment:
      - NODE_ENV=development
    command: npm start
